# Add project-specific ProGuard/R8 rules here as they become necessary.
# Nothing needed yet — release minification is off by default (see
# app/build.gradle.kts) until there's something worth shrinking.
